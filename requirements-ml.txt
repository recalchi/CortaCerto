# ML stack for Phase 1 — requires Python 3.11 (not 3.14)
# Install with: pip install -r requirements-ml.txt
#
# MediaPipe: real-time selfie segmentation (replaces GrabCut)
mediapipe==0.10.14

# YOLOv8: person detection + pose estimation
ultralytics>=8.2.0

# rembg: U2Net background removal (ONNX-based, high quality)
rembg[gpu]>=2.0.76

# ONNX runtime with GPU (CUDA) support — used by rembg + custom models
onnxruntime-gpu>=1.18.0

# Base stack (same as requirements.txt but pinned for 3.11)
customtkinter>=5.2.0
Pillow>=10.4.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
